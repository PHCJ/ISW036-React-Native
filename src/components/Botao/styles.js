import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    button: {
        margin: 8,
        width: 50,
        height:50,
    },
    buttonText: {
       fontSize:30,
       fontWeight:'bold',
       textAlign:'center',
    },
})


export default styles;